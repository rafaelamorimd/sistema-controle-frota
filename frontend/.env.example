# URL completa do backend (sem barra final)
# - Local com Vite dev server: deixe vazio (proxy /api e /storage no vite.config.ts)
# - Docker local: deixe vazio (mesmo proxy do Vite para API e arquivos em /storage)
# - Render/Producao: https://gefther-backend.onrender.com
VITE_API_URL=
