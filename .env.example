# Docker Compose Environment
APP_KEY=
DB_DATABASE=gefther
DB_USERNAME=gefther
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
REDIS_PASSWORD=
APP_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000
BACKEND_PORT=8000
FRONTEND_PORT=3000
# Dev local: docker compose usa Vite (override) — mudancas no ./frontend sem rebuild.
# Producao Docker: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
# Linux nativo (bind mount rapido): CHOKIDAR_USEPOLLING=false
